<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>File Upload Form</title>
    <link rel="stylesheet" href="styles/file-uploader.css" />
  </head>
  <body>
    <% if (typeof locals.file !== 'undefined') { %>
    <p>Uploaded: <%= locals.file.originalname %></p>
    <% } %>
    <h1>Upload a File</h1>
    <form action="/file" method="POST" enctype="multipart/form-data">
      <label for="uploaded_file">Choose a file:</label>
      <input type="file" id="uploaded_file" name="uploaded_file" required />
      <button type="submit">Upload</button>
    </form>
  </body>
</html>
